[project]
name = "litepaw"
version = "0.1.0"
description = "Lightweight QwenPaw memory service for blog deployment"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "websockets>=13.0",
    "reme>=0.4.0",
    "agentscope>=0.1.0",
    "pydantic>=2.0",
    "pyyaml>=6.0",
]
